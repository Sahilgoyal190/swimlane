import SwimLaneBoard from '@/features/swimLaneBoard';
import Layout from '@/components/Layout';

const Board = () => {
  return (
    <Layout title='Sprint board'>
      <SwimLaneBoard />
    </Layout>
  );
};

export default Board;
